enum ScreenStatus {
  loading,
  view,
  error,
}

enum HomeScreen {
  main,
  elders,
  search,
  favourites,
  setting,
}

enum SettingItemType {
  valueArrow,
  valueNotArrow,
  notValueArrow,
  valueActive,
  valueInactive,
  toggle,
  none,
  check,
}
